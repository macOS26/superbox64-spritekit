// SKEffectNode / SKCropNode definitions moved to SKStubs.swift (richer versions
// with shouldCenterFilter/shouldRasterize/blendMode, plus the proper SKBlendMode
// enum and SKShader pipeline). This file is left in place so SwiftPM keeps the
// target file list stable; remove once existing CI checkouts re-resolve.
